import { Box, Typography } from '@mui/material';
import React from 'react';

export const LetterHelper: React.FC = () => {
    return (
        <Box sx={{ p: 2, border: '1px solid', borderColor: 'primary.main', borderRadius: 2 }}>
            <Typography>Letter Helper component (to be implemented)</Typography>
        </Box>
    );
};